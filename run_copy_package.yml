- name: copy packages
  hosts: all
  become: true

  - name: mkdir /root/saa
    ansible.builtin.file:
      path: /root/sas
      state: director
     
  - name: first task
    ansible.builtin.copy:
      src: ./client/linux-x86_64
      dest: /root/sas
